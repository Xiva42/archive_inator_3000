# The Archive Inator 3000 !
> This archive algorithm is given a **imdb** link and will then insert the data containing the movie's *name*, *runtime*, *link* and relevent *tags* into a **google sheets** document.
